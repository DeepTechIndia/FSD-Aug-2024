// Modules =>

/* Export
import */

// 2 ways
// 1. Named Exports
// 2. Default Export

// 1.Named Export

export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
export const mul = (a, b) => a * b;
export const divide = (a, b) => a / b;
